<!DOCTYPE html>
<html>
  <%= render "layouts/head" %>

  <body>
    <%# パーシャルを呼び出す(コンポーネントを呼ぶ) %>
    <%= render "layouts/header" %>
    
    <div class="container">
      <%# yieldが、child %>
      <%= yield %>
      <%= render "layouts/footer"%>

      <%# 画面の下に出てくる.debugでcss編集 %>
      <%= debug(params) if Rails.env.development? %>
    </div>

  </body>
</html>
